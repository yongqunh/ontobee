The files under this ontobee/dev/ folder:

ontology_mysql.sql -- This MySQL script creates the table called "ontology" that is used to contain the information of the ontologies released in Ontobee.

load_rdf.php -- The loading program that loads an ontology to the Hegroup Virtuoso ontology RDF triple store that is used by Ontobee and OntoFox.
